---
description: SSH into the main Windows PC via Tailscale
---

SSH into the main PC using `ssh maints` (Tailscale) and execute the user's request. The main PC is a Windows machine accessible via Tailscale SSH as user `admin`.
