---
description: SSH into the server Windows PC via Tailscale
---

SSH into the server PC using `ssh serverts` (Tailscale) and execute the user's request. The server PC is a Windows machine accessible via Tailscale SSH as user `admin`.
